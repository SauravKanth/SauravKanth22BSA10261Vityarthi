def multiplicative_persistence(n):
    # 18. Steps until a single digit by multiplying digits
    steps = 0
    while n >= 10:
        prod = 1
        for d in str(n):
            prod *= int(d)
        n = prod
        steps += 1
    return steps
